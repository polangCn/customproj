server:
  port: 8102
spring:
  application:
    name: auth-service
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        enabled: true
        register-enabled: true
        server-addr: {{ nacos_servers.split() | join(',') }}
